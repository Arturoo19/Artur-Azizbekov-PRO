import { ImagenLibroPipe } from './imagen-libro-pipe';

describe('ImagenLibroPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenLibroPipe();
    expect(pipe).toBeTruthy();
  });
});
