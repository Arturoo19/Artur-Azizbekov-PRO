import { ImagenPizzaPipe } from './imagen-pizza-pipe';

describe('ImagenPizzaPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenPizzaPipe();
    expect(pipe).toBeTruthy();
  });
});
